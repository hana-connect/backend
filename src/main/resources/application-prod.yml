spring:
  config:
    import:
      - classpath:db.properties
      - classpath:db.properties

  datasource:
    username: hanaconnect

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
